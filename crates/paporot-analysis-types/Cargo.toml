[package]
name = "paporot-analysis-types"
version = "0.1.0"
edition = "2021"
description = "Shared type definitions for Paporot analysis pipeline (native + wasm)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
