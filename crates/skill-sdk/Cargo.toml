[package]
name = "paporot-skill-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for writing Paporot Skills as WASM modules"

[lib]
crate-type = ["lib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
